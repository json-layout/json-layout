{
  "description": "JSON Layout. Vocabulary and tools for rendering and edition of schematized JSON documents.",
  "main": "index.js",
  "scripts": {
    "test": "npm -w vocabulary test && npm -w core test",
    "build": "npm -w vocabulary run compile && npm -w vocabulary run build && npm -w core run build && npm -w examples run build",
    "lint": "npm -w vocabulary run lint && npm -w core run lint && npm -w examples run build",
    "prepare": "husky install",
    "dev": "run-p 'dev -w core' 'dev -w examples' 'dev -w vocabulary'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/json-layout/json-layout.git"
  },
  "keywords": [
    "json",
    "schema",
    "forms",
    "layout"
  ],
  "author": "Alban Mouton <alban.mouton@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/json-layout/json-layout/issues"
  },
  "homepage": "https://github.com/json-layout/json-layout#readme",
  "workspaces": [
    "vocabulary",
    "core",
    "examples"
  ],
  "devDependencies": {
    "@commitlint/cli": "^17.5.1",
    "@commitlint/config-conventional": "^17.4.4",
    "husky": "^8.0.3",
    "npm-run-all": "^4.1.5"
  }
}
