import { type JSONLayoutExample } from '..'

const example: JSONLayoutExample = {
  title: 'Text field - number',
  id: 'text-field-number',
  description: 'The default component used to render a number property is an adapted text field.',
  schema: {
    type: 'number',
    title: 'A simple number property'
  }
}

export default example
